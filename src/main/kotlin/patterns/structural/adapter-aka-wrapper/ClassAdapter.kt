/**
 * Kotlin/Java don't support multiple inheritance, making class adapters incompatible
 * with them. I prefer object adapters anyway, less of a maintenance headache.
 */
fun adaptClass(): String {
    val result = "woomp woomp"
    return result
}
